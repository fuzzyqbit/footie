---
context: phase
phase: 02-data-layer-cache
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-06-18 22:20 PT
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **FORCE_COLOR env** — `FORCE_COLOR=3` is set in this shell. Rich emits ANSI codes into captured output, breaking 5 `test_cli.py` plain-text assertions. Mitigation: run EVERY pytest invocation with `FORCE_COLOR= NO_COLOR=1` prefixed. (With it, the suite is green.)
- [ ] CONSTRAINT: **Must work in the worktree with a bootstrapped venv** — bg-isolation blocks edits in the shared checkout; a fresh worktree has no `.venv`. The worktree `.claude/worktrees/gsd-exec2` already exists with `.venv` installed (`-e ".[dev]"`). Re-enter it with EnterWorktree(path), use `.venv/bin/python -m pytest`.
- [ ] CONSTRAINT: **gsd-sdk not installed** — execute GSD steps manually (git + Write); no `gsd-sdk` query/commit helpers available.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Long Opus subagents drop the socket | planner / pattern-mapper / gsd-executor died on 30-67min runs (API socket closed) | blocking | Execute & plan INLINE; only spawn the gsd-plan-checker on Sonnet (short, reliable). Do not delegate execution to subagents. |
| Worktree from origin/main is stale | `ro` is 30+ commits ahead of origin/main; a fresh worktree maps old code | blocking | Always `git worktree add <dir> -b <branch> HEAD` (from current HEAD), never default base. |
| Forgetting FORCE_COLOR | running bare `pytest` shows 5 spurious CLI failures | advisory | Prefix `FORCE_COLOR= NO_COLOR=1`. |

<current_state>
Mid Plan 02-02 of Phase 2 (Data-Layer Cache & Batched Writes). In worktree `.claude/worktrees/gsd-exec2`, branch `gsd/exec-phase2`.

Plan 02-01 is COMMITTED (`7e252db`): `fc26/db.py` rewritten with process-global path-keyed cache (id index, immutable-snapshot reads, mtime+size invalidation) + batched/atomic/durable writes (`batch()`/`flush()` + fsync) + `tests/test_db_cache.py` (7 tests). 348 passed, golden byte-identical.

Plan 02-02 code is DONE and VERIFIED but UNCOMMITTED (will be in the WIP commit this handoff creates): `batch()` applied at `refresh_data` (wraps expand+enrich) and CLI `seed`/`sync`/`enrich`/`expand`/`images`; `tests/test_refresh.py` stub updated with a no-op `batch()`; `tests/benchmarks/test_bench_refresh.py` got a batched-bulk-upsert benchmark; new baseline `0002` saved.

Phase 2 is functionally complete + verified. Only bookkeeping + merge remain.
</current_state>

<completed_work>
- 02-01 (committed 7e252db): db.py cache/index/snapshot + batch/flush/fsync + test_db_cache.py — DONE.
- 02-02 Task 1 (uncommitted): apply batch at refresh_data + cli seed/sync/enrich/expand/images; test_refresh.py stub fix — DONE, 348 passed, golden byte-identical.
- 02-02 Task 2 (uncommitted): benchmarks run + new baseline 0002 saved — DONE.
</completed_work>

<remaining_work>
1. Write `.planning/phases/02-data-layer-cache/02-02-SUMMARY.md` (before/after numbers + fsync tradeoff + 26x batched win).
2. Commit Plan 02-02 cleanly (refresh.py, cli.py, test_refresh.py, test_bench_refresh.py, .benchmarks/0002, SUMMARY). NOTE: this pause bundles the uncommitted code into ONE wip commit — either amend/reword it to the proper `feat(02-02)` message or leave the wip commit and add SUMMARY on top.
3. Write phase `VERIFICATION.md` (status: passed) — mirror Phase 1's.
4. Update `.planning/STATE.md` (Phase 2 COMPLETE, ready for Phase 3), tick ROADMAP Phase 2 checkbox, set REQUIREMENTS DATA-01..04 → Complete (checkboxes + traceability rows).
5. From the MAIN checkout: `git -C <main> merge --ff-only gsd/exec-phase2`; then `git worktree remove --force .claude/worktrees/gsd-exec2` and `git branch -d gsd/exec-phase2`.
6. Report Phase 2 done; next is `/gsd:plan-phase 3` (API Responsiveness).
</remaining_work>

<decisions_made>
- Accept `upsert_one` +13% regression and re-baseline to 0002 — it is the intentional fsync(file+dir) durability cost on a single NON-batched write. Every real loop now batches (one fsync/run). Reads ~200x faster; batched refresh ~26x faster (971us vs 24919us); golden byte-identical. Net win overwhelming. The mean:10% gate vs 0001 fails ONLY on this one micro-op; that is expected and acceptable. Phase 3 compares against 0002.
- Cache process-global keyed by path.resolve(); find_all returns immutable snapshot (per-request repo construction + iterate-while-upsert).
- Inline execution + Sonnet checker only (Opus subagents kept dying).
</decisions_made>

<blockers>
- None blocking completion. (Env: use FORCE_COLOR= NO_COLOR=1.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-data-layer-cache/02-RESEARCH.md` — the design being implemented (anchored to `.planning/research/CACHING.md`).
2. `.planning/phases/02-data-layer-cache/02-01-PLAN.md` + `02-02-PLAN.md` — the plans + acceptance criteria.
3. `.planning/phases/02-data-layer-cache/02-01-SUMMARY.md` — what 02-01 shipped.
4. `.planning/phases/01-benchmark-equivalence-harness/VERIFICATION.md` — template for the Phase 2 VERIFICATION.md.

## Infrastructure State
- Worktree `.claude/worktrees/gsd-exec2` (branch `gsd/exec-phase2`) with `.venv` installed (pytest-benchmark 5.2.3, py-spy 0.4.2). `.venv` is gitignored.
- `.benchmarks/0001` = Phase 1 baseline (committed). `.benchmarks/0002` = post-Phase-2 baseline (uncommitted, will be in wip commit).
- `ro` HEAD = `64ddd80` (Phase 2 plans). `main` untouched (read-only per CLAUDE.md).

## Verification snapshot (already passing)
- `FORCE_COLOR= NO_COLOR=1 .venv/bin/python -m pytest -q` → 348 passed, 27 skipped.
- `FORCE_COLOR= NO_COLOR=1 .venv/bin/python -m pytest -m golden --run-bench -q` → 10 passed (byte-identical).
- Benchmarks: find_all 673us→3.3us, find_by_id 672us→3.4us, batched bulk_upsert 971us vs non-batch 24919us, api_meta 1343us→656us, api_cards 1867us→1181us.

<context>
The implementation is done and proven. This is a clean pause right before the SUMMARY/VERIFICATION/merge bookkeeping. Resume is low-risk: write docs, commit, merge. Keep the "zero behavior change" framing — the golden byte-identical gate is the contract and it passes.
</context>

<next_action>
Re-enter the worktree: EnterWorktree(path=".claude/worktrees/gsd-exec2"). Then write 02-02-SUMMARY.md, finalize the commit, write VERIFICATION.md, update STATE/ROADMAP/REQUIREMENTS, and FF-merge to ro + clean up.
</next_action>
