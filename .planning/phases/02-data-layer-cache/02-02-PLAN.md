---
phase: 02-data-layer-cache
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fc26/ingest/refresh.py
  - fc26/cli.py
  - .benchmarks/
autonomous: true
requirements: [DATA-03]
user_setup: []

must_haves:
  truths:
    - "A full refresh produces exactly one players.json write (one flush), not one-per-card"
    - "The refresh write benchmark is materially faster than the Phase 1 ro-HEAD baseline; no other benchmarked path regresses past +10%"
    - "Phase 1 golden stays byte-identical (refresh_players.json, API/CLI/build output unchanged)"
    - "All existing tests stay green; standalone fc26 enrich/expand/images/seed also batch"
  artifacts:
    - path: "fc26/ingest/refresh.py"
      provides: "refresh_data wraps expand+enrich in a single repo.batch()"
      contains: "batch()"
    - path: "fc26/cli.py"
      provides: "seed/enrich/expand/images CLI commands batch their upsert loops"
      contains: "batch()"
  key_links:
    - from: "fc26/ingest/refresh.py refresh_data"
      to: "CardRepository.batch"
      via: "with repo.batch(): expand_cards(...); enrich_cards(...)"
      pattern: "with repo.batch"
---

<objective>
Realize the O(n²)→O(n) refresh write win by wrapping the upsert loops in the `batch()` context added in Plan 01, then prove it with the Phase 1 harness (refresh benchmark faster, golden byte-identical) and re-baseline.

Purpose: DATA-03 realized — refresh writes once per run instead of rewriting the 4.4 MB file per card.
Output: batched `refresh_data` + CLI commands; refreshed committed benchmark baseline; before/after numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/CACHING.md
@.planning/phases/02-data-layer-cache/02-01-PLAN.md
@fc26/ingest/refresh.py
@fc26/cli.py
</context>

<threat_model>
No new runtime surface. The only correctness note: `batch()` must flush on exception so a partial scrape still persists (handled in Plan 01's contextmanager) — a mid-refresh crash leaves a consistent partial DB exactly as today (`cli.py:80-81` comment relies on this). Politeness/network unchanged (Phase 4's concern).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wrap ingest upsert loops in repo.batch()</name>
  <files>fc26/ingest/refresh.py, fc26/cli.py</files>
  <read_first>fc26/ingest/refresh.py, fc26/cli.py, fc26/db.py</read_first>
  <action>In `refresh_data` (`fc26/ingest/refresh.py:51-69`), wrap the `expand_cards(...)` + `enrich_cards(...)` calls in `with repo.batch():` so both inner upsert loops (`expand.py:69`, `enrich.py:86`, which receive the same `repo`) defer to one flush. Leave the manifest block after the batch (it only reads via find_by_id). In `fc26/cli.py`, wrap the upsert/ingest call in `with repo.batch():` for EVERY command that loops upserts: `seed` (the `for card in cards: repo.upsert(card)` loop at cli.py:82), `sync` (the `for card in cards: repo.upsert(card)` loop at cli.py:107-108 — ~100 cards from fetch_top100, so it benefits too), `enrich` (around the `enrich_cards(repo, ...)` call), `expand` (around `expand_cards(repo, ...)`), and `images` (around the `images.upgrade(...)` call whose serial main-thread upsert is `images.py:215`). Do NOT add batch() inside enrich.py/expand.py/images.py themselves (avoids non-re-entrant nesting; they already honor the repo's batch flag). The only command left unwrapped is `add` (cli.py:93) — a genuine single-card upsert where the default immediate flush is correct.</action>
  <verify>NO_COLOR=1 python -m pytest tests/test_refresh.py tests/test_expand.py tests/test_enrich.py tests/test_images.py tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - fc26/ingest/refresh.py contains "with repo.batch()" wrapping the expand+enrich body
    - fc26/cli.py contains "with repo.batch()" for seed/sync/enrich/expand/images (only `add` left unwrapped)
    - enrich.py/expand.py/images.py are NOT modified
    - `NO_COLOR=1 pytest tests/test_refresh.py tests/test_expand.py tests/test_enrich.py tests/test_images.py tests/test_cli.py -q` exits 0 (counts/new-vs-merged/id-suffix/single-writer invariants preserved)
  </acceptance_criteria>
  <done>Refresh + ingest CLI commands batch to a single write per run</done>
</task>

<task type="auto">
  <name>Task 2: Prove the win — benchmarks faster, golden byte-identical, re-baseline</name>
  <files>.benchmarks/</files>
  <read_first>tests/benchmarks/README.md, tests/benchmarks/test_bench_refresh.py, .planning/phases/01-benchmark-equivalence-harness/01-02-SUMMARY.md</read_first>
  <action>Confirm zero behavior change first: `NO_COLOR=1 pytest -m golden --run-bench` must pass byte-identical (refresh_players.json unchanged). Then run the benchmark suite and compare to the Phase 1 baseline: `NO_COLOR=1 pytest -m benchmark --run-bench --benchmark-storage=.benchmarks --benchmark-compare=0001 --benchmark-compare-fail=mean:10%`. The refresh (bulk_upsert) and find_by_id benches MUST be faster than baseline; the gate must not report a regression on any path. Record before/after means in the SUMMARY. Then capture a NEW post-Phase-2 baseline (`--benchmark-autosave`) and commit it under `.benchmarks/` as the reference Phase 3 will compare against (the bulk_upsert "regression" direction is irrelevant — we expect improvement; re-baselining locks the new floor). If the cache makes the corpus-size find benches too fast to be meaningful, note that the realistic signal is the `live` real-DB read bench (`--run-live`).</action>
  <verify>NO_COLOR=1 python -m pytest -m "golden or benchmark" --run-bench -q</verify>
  <acceptance_criteria>
    - `NO_COLOR=1 pytest -m golden --run-bench` exits 0 (byte-identical output preserved)
    - `NO_COLOR=1 pytest -m benchmark --run-bench --benchmark-compare=0001 --benchmark-compare-fail=mean:10%` exits 0 (no regression)
    - refresh bulk_upsert mean is lower than the 0001 baseline (recorded in SUMMARY)
    - a new post-Phase-2 baseline JSON is committed under .benchmarks/
  </acceptance_criteria>
  <done>Speedup proven against the harness; behavior byte-identical; new baseline committed</done>
</task>

</tasks>

<verification>
- [ ] `NO_COLOR=1 pytest -q` → all existing tests green
- [ ] `NO_COLOR=1 pytest -m golden --run-bench` → byte-identical
- [ ] `NO_COLOR=1 pytest -m benchmark --run-bench --benchmark-compare=0001 --benchmark-compare-fail=mean:10%` → green, refresh faster
- [ ] new baseline committed; before/after recorded in SUMMARY
</verification>

<success_criteria>
- All tasks completed
- Refresh writes once per run; measurable speedup vs baseline; nothing regressed
- Byte-identical output; all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-cache/02-02-SUMMARY.md`
</output>
