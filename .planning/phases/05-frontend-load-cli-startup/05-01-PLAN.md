---
phase: 05-frontend-load-cli-startup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.tsx
  - web/src/main.tsx
  - web/src/api/cards.ts
  - web/package.json
  - web/vite.config.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03]
user_setup: []

must_haves:
  truths:
    - "Every page is route-split via React.lazy at module top level, wrapped in one Suspense (fallback SkeletonGrid); the app still renders and routes identically (vitest + e2e green)"
    - "GeneratorPage is lazy so the @imgly WASM/ML chunk leaves the initial download (only fetched on /create + photo upload); the build emits a separate @imgly chunk, one chunk per page, and a smaller entry"
    - "The unused html-to-image dependency is removed from package.json (grep -rn html-to-image src returns nothing; PNG export still uses canvas.toDataURL)"
    - "QueryClient has defaultOptions (staleTime 5m, gcTime 30m, refetchOnWindowFocus false, retry 1) so navigation/focus no longer refetch static /api/meta + /api/cards; API data returned is unchanged"
  artifacts:
    - path: "web/src/App.tsx"
      provides: "lazy() page imports + Suspense boundary"
      contains: "lazy("
    - path: "web/src/main.tsx"
      provides: "QueryClient defaultOptions"
      contains: "defaultOptions"
    - path: "web/package.json"
      provides: "html-to-image removed"
      contains: "react-router-dom"
    - path: "web/vite.config.ts"
      provides: "vendor manualChunks (optional split)"
      contains: "build"
  key_links:
    - from: "App.tsx Suspense"
      to: "SkeletonGrid fallback"
      via: "lazy page chunks resolve behind one Suspense"
      pattern: "Suspense"
---

<objective>
Shrink the web app's initial download and stop redundant refetching — all behavior-preserving. Routes become per-page lazy chunks (the `@imgly` WASM dep leaves the entry), the dead `html-to-image` dep is removed, and React Query is tuned so navigation/focus don't refetch static data. Rendered pages + API data stay identical (vitest + e2e are the gate); the bundle bytes change by design.

Purpose: WEB-01 (code-split, smaller initial bundle), WEB-02 (lazy WASM dep + dead-dep removal), WEB-03 (React Query cache tuning).
Output: lazy `App.tsx`, tuned `main.tsx`, `cards.ts` cleanup, `package.json` dep removal, optional `vite.config.ts` vendor split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/FRONTEND-TOOLING.md
@.planning/phases/05-frontend-load-cli-startup/05-RESEARCH.md
@web/src/App.tsx
@web/src/main.tsx
@web/src/api/cards.ts
@web/vite.config.ts
@web/package.json
@web/src/App.test.tsx
</context>

<threat_model>
No new runtime surface. Removing the unused `html-to-image` dep shrinks supply-chain surface (net positive). `@imgly` stays behind its existing `await import()` — no eager WASM. React Query tuning + code-split change *when* data is fetched / *how* code loads, never *what* the API returns. No auth/secret/input changes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Route-level code-splitting (WEB-01 + WEB-02 WASM)</name>
  <files>web/src/App.tsx</files>
  <read_first>web/src/App.tsx, web/src/components/SkeletonGrid.tsx, web/src/App.test.tsx, .planning/research/FRONTEND-TOOLING.md (§A1/A2)</read_first>
  <action>
Rewrite `App.tsx` to load each page via `React.lazy` at module top level (NOT inside the component — react.dev warns it resets state every render) and wrap `<Routes>` in a single `<Suspense fallback={<SkeletonGrid />}>`:
- Imports: `import { Routes, Route, Navigate } from 'react-router-dom'`; `import { lazy, Suspense } from 'react'`; keep `import Sidebar from './components/Sidebar'`; add `import SkeletonGrid from './components/SkeletonGrid'`.
- Replace the 11 static page imports (`App.tsx:3-13`) with `const CardsPage = lazy(() => import('./pages/CardsPage'))` … for all 11 pages (CardsPage, SquadsPage, BuildPage, UpgradePage, UpdatesPage, ValuePage, WatchlistPage, ComparePage, ObjectivesPage, SbcsPage, GeneratorPage). All 11 are default exports (verified) so `lazy(() => import(...))` works directly.
- Keep the exact same `<Routes>`/`<Route>` block (paths + element components unchanged), but wrap it: `<Suspense fallback={<SkeletonGrid />}><Routes>…</Routes></Suspense>`. Keep the outer `<div className="flex h-screen …"><Sidebar /><main …>…</main></div>` layout byte-for-byte.
- Do NOT touch GeneratorPage — its `await import('@imgly/background-removal')` (`GeneratorPage.tsx:272`) already lazy-loads the WASM; making the page lazy (here) is what removes that graph from the entry chunk.
  </action>
  <verify>cd web && npm run test 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - App.tsx uses `lazy(` for all 11 pages and a single `Suspense` with `SkeletonGrid` fallback
    - the `<Routes>`/`<Route>` paths + element components are unchanged
    - `cd web && npm run test` green (App.test routes via async findBy*; page tests unaffected)
  </acceptance_criteria>
  <done>Pages are lazy chunks behind one Suspense; @imgly leaves the entry</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead html-to-image dep + optional vendor split (WEB-02)</name>
  <files>web/package.json, web/vite.config.ts</files>
  <read_first>web/package.json, web/vite.config.ts, .planning/research/FRONTEND-TOOLING.md (§A3/A4)</read_first>
  <action>
1. Confirm `html-to-image` is unused: `grep -rn "html-to-image" web/src` → must be empty (PNG export uses `canvas.toDataURL`, `GeneratorPage.tsx:46,107,402`). Remove the `"html-to-image": "^1.11.13"` line from `web/package.json` `dependencies`. (After removal, `npm install` updates the lockfile.)
2. (Optional, low-risk) Add a vendor `manualChunks` to `vite.config.ts` inside `defineConfig`, ALONGSIDE the existing `plugins`/`server`/`test` keys: `build: { rollupOptions: { output: { manualChunks: { 'react-vendor': ['react','react-dom','react-router-dom'], 'query-vendor': ['@tanstack/react-query'] } } } }`. Vite 6 = Rollup → use `rollupOptions` (NOT rolldownOptions). OBJECT form only. Do NOT function-form route-split (React.lazy owns page chunks) and do NOT add `@imgly` to any group (keep it behind the dynamic import). If the build warns or chunks look wrong, drop this optional step — Task 1 is the core WEB win.
  </action>
  <verify>cd web && grep -rn "html-to-image" src; echo "(empty above = removed)"; npm install >/dev/null 2>&1 && npm run build 2>&1 | tail -30</verify>
  <acceptance_criteria>
    - `grep -rn html-to-image web/src` is empty; the dep is gone from package.json
    - `cd web && npm run build` succeeds (tsc -b + vite build)
    - chunk list shows: a smaller entry, one chunk per page, a separate `@imgly` chunk (reachable only from the generator page), and (if kept) react-vendor/query-vendor
  </acceptance_criteria>
  <done>Dead dep removed; build emits per-page + separate @imgly chunks</done>
</task>

<task type="auto">
  <name>Task 3: React Query cache tuning (WEB-03)</name>
  <files>web/src/main.tsx, web/src/api/cards.ts</files>
  <read_first>web/src/main.tsx, web/src/api/cards.ts, web/src/App.test.tsx, .planning/research/FRONTEND-TOOLING.md (§A6)</read_first>
  <action>
In `main.tsx`, replace `const queryClient = new QueryClient()` with:
`const queryClient = new QueryClient({ defaultOptions: { queries: { staleTime: 5 * 60 * 1000, gcTime: 30 * 60 * 1000, refetchOnWindowFocus: false, retry: 1 } } })`.
Then in `cards.ts`, the per-query `staleTime: 5 * 60 * 1000` on `useAllCards` (`cards.ts:53`) is now redundant with the global default — remove that one line (keep the queryKey + queryFn). Leave `useCards` unchanged. This changes *when* queries refetch, not *what* `/api/cards`/`/api/meta` return. `App.test.tsx` constructs its own QueryClient (retry:false) so the global default doesn't affect tests.
  </action>
  <verify>cd web && npm run test 2>&1 | tail -15</verify>
  <acceptance_criteria>
    - main.tsx QueryClient has `defaultOptions.queries` with staleTime/gcTime/refetchOnWindowFocus:false/retry
    - cards.ts `useAllCards` no longer sets its own staleTime (relies on the global default)
    - `cd web && npm run test` green (no test asserts refetch-on-focus)
  </acceptance_criteria>
  <done>Navigation/focus no longer refetch static data; API data identical</done>
</task>

</tasks>

<verification>
- [ ] `cd web && npm run test` → vitest green (render + routing under lazy unchanged)
- [ ] `cd web && npm run build` → succeeds; entry smaller; per-page chunks + separate @imgly chunk
- [ ] `grep -rn html-to-image web/src` empty; dep removed from package.json
- [ ] (if a serve env is available) `cd web && npm run e2e` green (content-waited, resilient to Suspense fallback)
- [ ] No change to rendered output or API data — only load timing/bundle shape
</verification>

<success_criteria>
- Routes code-split; initial JS bundle materially smaller than the ~298 KB baseline
- @imgly only in its own chunk; html-to-image removed
- React Query tuned; no redundant refetch; behavior identical (vitest/e2e green)
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-load-cli-startup/05-01-SUMMARY.md` recording before/after entry + chunk sizes from `npm run build`.
</output>
