---
phase: 05-frontend-load-cli-startup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fc26/ingest/constants.py
  - fc26/ingest/refresh.py
  - fc26/cli.py
  - tests/test_cli.py
  - tests/test_cli_startup.py
autonomous: true
requirements: [CLI-01]
user_setup: []

must_haves:
  truths:
    - "Importing fc26.cli no longer pulls the scrape/builder graph: selectolax and httpx are NOT in sys.modules after `import fc26.cli` (proven by a subprocess test)"
    - "Heavy imports (.builder.*, .chem.*, .ingest.* scrapers, httpx) are deferred into the command bodies that use them; light deps (typer, rich, .db, .errors, .models, option-default constants) stay at module top"
    - "The DEFAULT_MIN_OVR / DEFAULT_INTERVAL_HOURS option defaults come from a new light fc26/ingest/constants.py (not from .ingest.refresh, which transitively pulls selectolax/httpx)"
    - "CLI output is byte-identical: tests/test_cli.py (62) green and the cli_list/cli_show goldens unchanged"
  artifacts:
    - path: "fc26/ingest/constants.py"
      provides: "light home for refresh defaults"
      contains: "DEFAULT_MIN_OVR"
    - path: "fc26/cli.py"
      provides: "deferred heavy imports in command bodies"
      contains: "from .ingest.constants import"
    - path: "tests/test_cli_startup.py"
      provides: "subprocess import-cost assertion"
      min_lines: 12
  key_links:
    - from: "import fc26.cli"
      to: "selectolax/httpx NOT loaded"
      via: "deferred command-body imports"
      pattern: "sys.modules"
---

<objective>
Make `fc26 --help` and light commands (`search`/`show`/`list`) start fast by deferring the scrape+builder+chem import graph (which pulls `selectolax` + `httpx`) into the command bodies that actually use it — output and behavior byte-identical.

Purpose: CLI-01 (deferred heavy imports → fast CLI startup).
Output: new `fc26/ingest/constants.py`, refactored `fc26/cli.py` (deferred imports), `tests/test_cli_startup.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/FRONTEND-TOOLING.md
@.planning/phases/05-frontend-load-cli-startup/05-RESEARCH.md
@fc26/cli.py
@fc26/ingest/refresh.py
@fc26/api/app.py
@tests/test_cli.py
@tests/conftest.py
</context>

<threat_model>
No new runtime surface. Deferred imports change *when* a module loads, not *what* a command does — identical output, identical `FC26Error` handling. No secrets/input/network change. The new `constants.py` holds two plain literals (no heavy imports).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — subprocess import-cost test (CLI-01 gate)</name>
  <files>tests/test_cli_startup.py</files>
  <read_first>tests/conftest.py, fc26/cli.py</read_first>
  <action>
Create `tests/test_cli_startup.py` (plain default-suite test, no marker). It runs a fresh subprocess that imports `fc26.cli` and asserts the heavy deps are NOT loaded:
```python
import subprocess, sys

def test_cli_import_does_not_pull_heavy_deps():
    code = (
        "import sys, fc26.cli; "
        "heavy = {'selectolax', 'httpx'} & set(sys.modules); "
        "assert not heavy, f'cli import pulled heavy deps: {heavy}'"
    )
    r = subprocess.run([sys.executable, "-c", code], capture_output=True, text=True)
    assert r.returncode == 0, r.stderr
```
Use a subprocess (not in-process) so an already-imported `selectolax`/`httpx` from other tests doesn't pollute `sys.modules`. This test is RED until Tasks 2-3 land; that's expected (Wave 0).
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_cli_startup.py -q || echo "(expected RED until deferral lands)"</verify>
  <acceptance_criteria>
    - tests/test_cli_startup.py exists; runs `import fc26.cli` in a subprocess; asserts selectolax + httpx absent from sys.modules
    - it is a default-suite test (no marker)
  </acceptance_criteria>
  <done>The CLI-01 success condition is encoded as an automated test</done>
</task>

<task type="auto">
  <name>Task 2: Extract refresh defaults into a light constants module</name>
  <files>fc26/ingest/constants.py, fc26/ingest/refresh.py</files>
  <read_first>fc26/ingest/refresh.py, fc26/api/app.py</read_first>
  <action>
The option defaults `DEFAULT_MIN_OVR`/`DEFAULT_INTERVAL_HOURS` are needed at cli decoration time, but they currently live in `refresh.py` — importing them pulls the whole ingest graph (refresh→enrich/expand/web_async→selectolax+httpx). Move the literals to a light module:
- New `fc26/ingest/constants.py` (NO heavy imports — only `from __future__ import annotations`):
  ```python
  DEFAULT_MIN_OVR = 84
  DEFAULT_INTERVAL_HOURS = 72.0   # every 3 days — gentle on the source sites
  ```
- In `refresh.py`, REPLACE the two literal definitions with `from .constants import DEFAULT_INTERVAL_HOURS, DEFAULT_MIN_OVR` (re-export — so existing `from .ingest.refresh import DEFAULT_MIN_OVR, DEFAULT_INTERVAL_HOURS` in `app.py` and tests keeps working unchanged). Keep `jittered_sleep`, `refresh_data`, `refresh_data_async`, `RefreshResult`, `MANIFEST_CARD_CAP` where they are.
- `fc26/ingest/__init__.py` is empty, so `from fc26.ingest.constants import …` is light (verified). Do NOT add imports to `__init__.py`.
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_refresh.py tests/test_api.py -q && python -c "import sys; import fc26.ingest.constants; print('selectolax' in sys.modules, 'httpx' in sys.modules)"</verify>
  <acceptance_criteria>
    - fc26/ingest/constants.py defines DEFAULT_MIN_OVR + DEFAULT_INTERVAL_HOURS with no heavy imports
    - refresh.py re-exports them from .constants; `from fc26.ingest.refresh import DEFAULT_MIN_OVR, DEFAULT_INTERVAL_HOURS` still works (app.py + tests green)
    - `import fc26.ingest.constants` does not pull selectolax/httpx (prints `False False`)
  </acceptance_criteria>
  <done>Option defaults live in a light module the CLI can import cheaply</done>
</task>

<task type="auto">
  <name>Task 3: Defer the heavy imports in cli.py into command bodies (CLI-01)</name>
  <files>fc26/cli.py</files>
  <read_first>fc26/cli.py (all), .planning/research/FRONTEND-TOOLING.md (§B4), fc26/cli.py command line map below</read_first>
  <action>
Rework `cli.py` imports. KEEP at module top (light, used broadly / needed at decoration time): `from __future__ import annotations`, `import asyncio`, `import json as json_lib`, `from dataclasses import asdict`, `from pathlib import Path`, `from typing import NoReturn`, `import typer`, `from rich.console import Console`, `from rich.table import Table`, `from .db import CardRepository, card_to_dict`, `from .errors import FC26Error`, `from .models import Card`, and `from .ingest.constants import DEFAULT_INTERVAL_HOURS, DEFAULT_MIN_OVR`. Keep `app = typer.Typer(...)`, `console = Console(width=200)`, `DEFAULT_DB`/`DB_OPTION`/`JSON_FLAG`, and `_fail`.

REMOVE these top-level imports — **all confirmed dead in cli.py as of this writing** (Phase 4 wired the async paths and left the sync ones unused): `import time` (Phase 4 dropped `sleep=time.sleep`), `from .ingest.web import fetch_html`, `jittered_sleep` + sync `refresh_data` (from the `.ingest.refresh` import group), and the sync `enrich_cards`/`expand_cards`/`upgrade_card_images`. `import httpx` and `from .ingest.web_async import AsyncFetcher` are not dead — they MOVE into command bodies (see the map below). Do a confirming `grep -n "<symbol>" fc26/cli.py` for each "dead" symbol before deleting; if (unexpectedly) a stray use remains, defer that import into the using command's body instead of deleting. Keep the `.ingest.refresh` import group ONLY for `refresh_data_async`? No — `refresh_data_async` also moves into the `refresh` body, so the entire `from .ingest.refresh import (...)` block is removed from the top (DEFAULT_* now come from `.ingest.constants`).

DEFER (add a local `from … import …` at the TOP of each listed command body — verified usage map):
| Import | Command body(s) that need it |
|--------|------------------------------|
| `from .ingest.seed import seed_cards` | `seed` (uses at :85) |
| `from .ingest.futgg import fetch_futgg_card` | `add` (:101) |
| `from .ingest.fcratings import fetch_top100` | `sync` (:112) |
| `from .ingest.web_async import AsyncFetcher` + `from .ingest.enrich import enrich_cards_async` | `enrich` (:202-203) |
| `from .ingest.web_async import AsyncFetcher` + `from .ingest.expand import expand_cards_async` | `expand` (:236-237) |
| `from .ingest.web_async import AsyncFetcher` + `from .ingest.images import upgrade_card_images_async` | `images` (:278-279) |
| `from .ingest.refresh import refresh_data_async` | `refresh` (:318) |
| `import httpx` + `from .ingest.objectives import write_objectives` | `refresh_objectives` (:345,348) |
| `import httpx` + `from .ingest.sbc import write_sbcs` | `refresh_sbcs` (:364,367) |
| `from .builder.market import parse_budget` + `from .chem.lineup import load_lineup, resolve_cards` + `from .builder.upgrade import find_upgrades` | `upgrade` (:387-391) |
| `from .builder.market import parse_budget` + `from .builder.build import build_squad` + `from .chem.engine import compute_chemistry` | `build` (:450-455) |
| `from .builder.market import parse_budget` + `from .chem.lineup import load_lineup, resolve_cards` + `from .builder.plan import plan_for_squad, plan_from_scratch` | `plan` (:509-517) |
| `from .chem.lineup import load_lineup, resolve_cards` + `from .chem.engine import compute_chemistry` | `chem` (:591-595) |
| `from .chem.lineup import load_lineup, resolve_cards` + `from .builder.advise import advise_squad` | `advise` (:628) |
| `from .chem.lineup import load_lineup, resolve_cards` + `from .chem.engine import compute_chemistry` + `from .builder.boost import boosted_stats` | `boost` (:672-679) |

`serve` already defers `uvicorn`/`create_app` (`cli.py:715-716`) — leave it. Place each deferred import as the FIRST line(s) inside the function body (after the docstring). Do not change any logic, output strings, option signatures, or error handling. If a symbol is used by multiple commands, import it locally in EACH (cheap; clarity over DRY for lazy imports).

**Leave `console = Console(width=200)` at module top** — the research notes lazifying it as a lower-priority follow-on; it is NOT in scope for CLI-01 (the gate only requires selectolax + httpx absent at import).

**REQUIRED test fix (else 12 existing test_cli.py tests fail with AttributeError):** those tests `monkeypatch.setattr("fc26.cli.<symbol>", fake)` for symbols this task removes from the `fc26.cli` namespace. Because each is now a local `from .ingest.X import <symbol>` inside the command body (resolved at call time), retarget the patch to the SOURCE module so the fake is in place when the body imports it. In `tests/test_cli.py` replace (use replace_all per symbol):
- `"fc26.cli.fetch_futgg_card"` → `"fc26.ingest.futgg.fetch_futgg_card"` (lines 123, 146, 184)
- `"fc26.cli.fetch_top100"` → `"fc26.ingest.fcratings.fetch_top100"` (line 134)
- `"fc26.cli.enrich_cards_async"` → `"fc26.ingest.enrich.enrich_cards_async"` (lines 198, 212, 223, 235, 246)
- `"fc26.cli.expand_cards_async"` → `"fc26.ingest.expand.expand_cards_async"` (lines 260, 278, 289)
This is a patch-target change only — the fake stubs, assertions, exit codes, and expected output strings are all UNCHANGED (behavior contract preserved). No other test_cli.py edits. (images/refresh commands have no monkeypatched tests, so nothing to retarget there.)
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_cli.py tests/test_cli_startup.py -q && FORCE_COLOR= NO_COLOR=1 python -m pytest -m golden --run-bench -k cli -q</verify>
  <acceptance_criteria>
    - cli.py top-level imports are only the light set listed; no `.builder.*`/`.chem.*`/`.ingest.*` scraper imports or `httpx` at module top
    - the 12 monkeypatch targets in tests/test_cli.py are retargeted to source modules (fetch_futgg_card/fetch_top100/enrich_cards_async/expand_cards_async); stubs + assertions unchanged
    - `tests/test_cli_startup.py` passes (selectolax + httpx absent after `import fc26.cli`)
    - `tests/test_cli.py` 62 tests green; cli_list/cli_show goldens byte-identical
    - `python -X importtime -m fc26 --help` shows the heavy modules no longer loaded (record before/after in summary)
  </acceptance_criteria>
  <done>CLI startup deferred; output byte-identical</done>
</task>

</tasks>

<verification>
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest tests/test_cli.py tests/test_cli_startup.py -q` green
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest -m golden --run-bench` → cli goldens byte-identical
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest -q` → whole suite green (refresh/api unaffected by the constants move)
- [ ] `import fc26.cli` pulls neither selectolax nor httpx; `--help` importtime improved
</verification>

<success_criteria>
- Heavy graph deferred; `fc26 --help`/`search`/`show`/`list` start without loading selectolax/httpx
- CLI behavior + text byte-identical (test_cli + golden)
- DEFAULT_* defaults sourced from a light constants module
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-load-cli-startup/05-02-SUMMARY.md` recording the `python -X importtime -m fc26 --help` before/after (which heavy modules dropped).
</output>
