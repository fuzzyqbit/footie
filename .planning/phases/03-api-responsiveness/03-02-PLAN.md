---
phase: 03-api-responsiveness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - fc26/builder/upgrade.py
  - .benchmarks/
autonomous: true
requirements: [API-02]
user_setup: []

must_haves:
  truths:
    - "find_upgrades produces a byte-identical UpgradePlan (every swap, order, deltas, spent, before/after scores, warnings) — Phase 1 golden builder_matrix + api_build + api_upgrade unchanged"
    - "The inner loop iterates per-position eligible buckets (priced + position-eligible, original order preserved) instead of the full 2,434-card pool every round"
    - "meta_score is read from a precomputed (card_id, position) table; the current-XI slug list is precomputed per round and the _same_player PREFIX semantics are preserved exactly"
    - "compute_chemistry's algorithm is unchanged; the candidate tie-break order (delta==best and cost<best.net_cost) is preserved; build/upgrade benchmarks are faster than the 0002 baseline with no regression elsewhere"
  artifacts:
    - path: "fc26/builder/upgrade.py"
      provides: "find_upgrades with hoisted invariants (eligible buckets, meta_score table, per-round slug list)"
      contains: "eligible"
  key_links:
    - from: "find_upgrades inner loop"
      to: "eligible-by-position buckets"
      via: "order-preserving precompute, iterate bucket[position]"
      pattern: "eligible"
---

<objective>
Hoist the invariant work out of the `find_upgrades` greedy loop so build/upgrade get materially faster — while producing byte-identical output. This is the only equivalence-sensitive change in Phase 3; it is gated by the Phase 1 golden (builder_matrix, api_build, api_upgrade).

Purpose: API-02 (hot-loop hoisting). Output: optimized `fc26/builder/upgrade.py` + refreshed benchmark baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/BACKEND-COMPUTE.md
@.planning/phases/03-api-responsiveness/03-RESEARCH.md
@fc26/builder/upgrade.py
@fc26/builder/meta.py
@fc26/chem/formations.py
</context>

<threat_model>
No runtime surface change. The risk is purely output-equivalence: a reorder or a broken prefix check would change which swap the greedy picks. Mitigations: order-preserving filters only; keep `_same_player` prefix logic; golden builder_matrix/api_build/api_upgrade are the hard gate (byte-identical) before this ships.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Hoist invariants in find_upgrades (order + prefix preserving)</name>
  <files>fc26/builder/upgrade.py</files>
  <read_first>fc26/builder/upgrade.py, fc26/builder/meta.py, fc26/chem/formations.py, .planning/research/BACKEND-COMPUTE.md</read_first>
  <action>In `find_upgrades`, before the round loop, precompute (all order-preserving, pure):
  (a) `eligible[position] -> tuple[Card,...]`: iterate `pool` ONCE in original order, keep cards with `price is not None` AND (position == card.position or position in card.alt_positions), bucketed per position that appears in `lineup.slots`. The inner `for candidate in pool` loop becomes `for candidate in eligible[position]` — same candidates, same relative order, so the price/position `continue` checks (upgrade.py:114-117) are now redundant (already filtered). This is the big win (~hundreds not 2,434 per slot per round).
  (b) `meta_cache[(card_id, position)] -> meta_score`: precompute meta_score for each (eligible card, position) pair once (meta_score is pure, meta.py:41); read it for both `out_meta` (:112) and `candidate_meta` (:129) instead of recomputing each round. Candidates with `meta_score is None` are still skipped identically (precompute None and keep the `if candidate_meta is None: continue` guard, OR exclude None-meta cards from the bucket — but only if that preserves which candidates are considered; SAFER: keep them in the bucket and keep the None-skip guard to match original control flow exactly).
  (c) Per round: precompute `current_names = [card.player_name for slot,card in current.items()]` (or their slugs). The `_same_player` check (:120-124) excludes the slot under test — so build the comparison list as the current XI EXCLUDING `slot_key`, and run the EXISTING `_same_player(candidate.player_name, name)` prefix check against it. Keep `_same_player` and its prefix semantics (:62-64) verbatim — do NOT replace with plain slug-set membership. With slugify memoized (Plan 01) this is already cheap; the hoist just avoids rebuilding the comparison list per candidate.
  CRITICAL: preserve candidate ITERATION ORDER (the tie-break at :137-139 depends on it) and the exact skip conditions. compute_chemistry and _squad_state are UNCHANGED. Do not sort/reorder candidates.</action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_upgrade.py tests/test_build.py tests/test_api.py -q</verify>
  <acceptance_criteria>
    - upgrade.py builds per-position eligible buckets from the pool (order-preserving) and the inner loop iterates a bucket, not the full pool
    - meta_score is read from a precomputed table, not recomputed per round
    - _same_player and its prefix logic (:62-64) are unchanged
    - candidate order + tie-break (:137-139) preserved
    - `FORCE_COLOR= NO_COLOR=1 pytest tests/test_upgrade.py tests/test_build.py tests/test_api.py -q` exits 0
  </acceptance_criteria>
  <done>find_upgrades hoisted; existing build/upgrade tests green</done>
</task>

<task type="auto">
  <name>Task 2: Prove byte-identical + faster, re-baseline</name>
  <files>.benchmarks/</files>
  <read_first>tests/benchmarks/README.md, .planning/phases/02-data-layer-cache/02-02-SUMMARY.md</read_first>
  <action>Run the golden equivalence gate first: `FORCE_COLOR= NO_COLOR=1 pytest -m golden --run-bench` MUST pass byte-identical (builder_matrix.json, api_build.json, api_upgrade.json unchanged — proves the hoist changed nothing observable). Then the benchmark compare vs the Phase 2 baseline: `FORCE_COLOR= NO_COLOR=1 pytest -m benchmark --run-bench --benchmark-storage=.benchmarks --benchmark-compare=0002 --benchmark-compare-fail=mean:10%`; `find_upgrades`, `/api/build`, `/api/upgrade` MUST be faster, nothing regressed. Record before/after means in the SUMMARY. Capture a new post-Phase-3 baseline (`--benchmark-autosave`) and commit it under `.benchmarks/` (the reference Phase 4 compares against). If golden FAILS, the hoist changed behavior — stop and fix (do NOT regenerate the golden).</action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest -m "golden or benchmark" --run-bench -q</verify>
  <acceptance_criteria>
    - `FORCE_COLOR= NO_COLOR=1 pytest -m golden --run-bench` exits 0 (byte-identical)
    - benchmark compare vs 0002 shows find_upgrades / api_build / api_upgrade faster, no regression
    - a new post-Phase-3 baseline JSON committed under .benchmarks/
  </acceptance_criteria>
  <done>Speedup proven, output byte-identical, new baseline committed</done>
</task>

</tasks>

<verification>
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest -q` → all tests green
- [ ] `pytest -m golden --run-bench` → byte-identical (builder_matrix, api_build, api_upgrade)
- [ ] benchmark vs 0002 → build/upgrade faster, no regression; new baseline committed
</verification>

<success_criteria>
- All tasks completed
- find_upgrades hoisted; build/upgrade faster; output byte-identical
- compute_chemistry algorithm untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-responsiveness/03-02-SUMMARY.md`
</output>
