---
phase: 03-api-responsiveness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fc26/models.py
  - fc26/chem/aliases.py
  - fc26/api/app.py
  - tests/test_api_perf.py
autonomous: true
requirements: [API-01, API-02, API-03]
user_setup: []

must_haves:
  truths:
    - "GET read endpoints run off the event loop (plain def → anyio threadpool); POST/PUT offload blocking compute via run_in_threadpool — a slow /api/build no longer serializes concurrent requests"
    - "slugify and canonical_league/nation/club are memoized (lru_cache); compute_chemistry/_same_player/make_card_id get faster automatically with NO algorithm change"
    - "/api/meta is cached keyed on the DB file mtime (per resolved path), busted on refresh, and byte-identical (versions unfiltered; leagues/nations/clubs filter falsy)"
    - "All existing tests green; Phase 1 golden (api_meta, api_build, api_upgrade, builder_matrix) byte-identical"
  artifacts:
    - path: "fc26/models.py"
      provides: "lru_cache on slugify"
      contains: "lru_cache"
    - path: "fc26/chem/aliases.py"
      provides: "lru_cache on canonical_league/nation/club"
      contains: "lru_cache"
    - path: "fc26/api/app.py"
      provides: "sync GET handlers; run_in_threadpool POSTs; /api/meta result cache + refresh bust"
      contains: "run_in_threadpool"
    - path: "tests/test_api_perf.py"
      provides: "memoization + /api/meta cache tests"
      min_lines: 30
  key_links:
    - from: "get_meta"
      to: "_META_CACHE keyed on db_path.resolve()+mtime_ns"
      via: "cache lookup, busted on refresh"
      pattern: "_META_CACHE|mtime"
---

<objective>
Land the zero-risk API responsiveness wins: move blocking work off the event loop, memoize the hot leaf functions, and cache `/api/meta` — all output-identical (Phase 1 golden is the gate). `compute_chemistry`'s algorithm is untouched.

Purpose: API-01 (responsiveness under concurrency), API-02 leaf-memoization (single-request latency), API-03 (`/api/meta` cost).
Output: memoized `slugify`/`canonical_*`, threaded handlers, cached `/api/meta`, `tests/test_api_perf.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/BACKEND-COMPUTE.md
@.planning/phases/03-api-responsiveness/03-RESEARCH.md
@fc26/api/app.py
@fc26/models.py
@fc26/chem/aliases.py
</context>

<threat_model>
No new runtime surface. Moving handlers to the threadpool adds shared-state concurrency: mitigated — Phase 2 repo cache is RLock-guarded + immutable snapshots; the new `/api/meta` cache uses its own `threading.Lock`; `lru_cache` is thread-safe in CPython. No secrets, no new I/O.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Memoize slugify + canonical_* (API-02 leaves)</name>
  <files>fc26/models.py, fc26/chem/aliases.py</files>
  <read_first>fc26/models.py, fc26/chem/aliases.py, .planning/research/BACKEND-COMPUTE.md</read_first>
  <action>Add `from functools import lru_cache` and decorate `slugify` (`models.py:98`) with `@lru_cache(maxsize=None)`. In `aliases.py`, decorate `canonical_league`, `canonical_nation`, `canonical_club` with `@lru_cache(maxsize=None)` (add the import). These are pure `str→str` maps over a bounded vocabulary; memoizing cannot change output. Do NOT change their bodies. Confirm all call sites pass `str` (they do — call sites guard with `if card.club:` etc.).</action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_db.py tests/test_chem.py tests/test_upgrade.py tests/test_build.py -q</verify>
  <acceptance_criteria>
    - models.py and aliases.py both contain "lru_cache"
    - `python -c "from fc26.models import slugify; from fc26.chem.aliases import canonical_club; assert slugify.cache_info() is not None and canonical_club.cache_info() is not None"` exits 0
    - chem/upgrade/build/db tests pass (output unchanged)
  </acceptance_criteria>
  <done>Hot leaf functions memoized; behavior identical</done>
</task>

<task type="auto">
  <name>Task 2: Get handlers off the event loop (API-01)</name>
  <files>fc26/api/app.py</files>
  <read_first>fc26/api/app.py, tests/test_api.py, .planning/research/BACKEND-COMPUTE.md</read_first>
  <action>Add `from starlette.concurrency import run_in_threadpool`. Convert the GET read endpoints from `async def` to plain `def` so Starlette runs them in the anyio threadpool: `list_cards` (157), `get_card` (219), `list_squads` (226), `get_squad` (231), `get_meta` (328), `get_objectives` (345), `get_sbcs` (371), `get_updates` (384), `get_value` (415). Leave `serve_spa` and the async exception handlers async. For the POST/PUT handlers that `await request.json()` (`put_squad` 245, `post_chem` 256, `post_boost` 265, `post_upgrade` 279, `post_build` 293): KEEP them `async def`, read the body with `await request.json()`, then run the remaining blocking compute via `await run_in_threadpool(_worker, ...)` where `_worker` is a local sync function containing the existing body verbatim (repo construction, find_all, build_squad/find_upgrades/compute_chemistry, response assembly). FC26Error raised inside the worker propagates through run_in_threadpool to the existing exception handlers — do not change error handling. Do NOT alter any computed values or response shapes.</action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_api.py tests/test_web.py -q</verify>
  <acceptance_criteria>
    - app.py contains "run_in_threadpool"
    - GET handlers list_cards/get_card/get_meta/get_value/get_objectives/get_sbcs/get_updates/list_squads/get_squad are plain `def` (not `async def`)
    - POST/PUT handlers remain `async def` and call run_in_threadpool for the blocking body
    - `FORCE_COLOR= NO_COLOR=1 pytest tests/test_api.py tests/test_web.py -q` exits 0 (responses identical)
  </acceptance_criteria>
  <done>Blocking work off the event loop; responses byte-identical</done>
</task>

<task type="auto">
  <name>Task 3: Cache /api/meta + tests (API-03 + API-02/API-01 tests)</name>
  <files>fc26/api/app.py, tests/test_api_perf.py</files>
  <read_first>fc26/api/app.py, tests/test_api.py, .planning/research/BACKEND-COMPUTE.md</read_first>
  <action>Add a module-level `_META_CACHE: dict[Path, tuple[int, dict]] = {}` + `_META_LOCK = threading.Lock()` in app.py (keyed by `db_path.resolve()` so multiple apps/tests don't collide). Refactor `get_meta` to compute the dict from `CardRepository(db_path).find_all()` and cache it keyed on `(resolve, st_mtime_ns)`; on a cache hit with matching mtime, return the cached dict. PRESERVE the exact predicates: `leagues`/`nations`/`clubs` filter falsy, `versions` does NOT filter; `formations` from FORMATIONS, `styles` from available_styles(). In the auto-refresh loop (`_refresh_loop`, after a successful `refresh_data`, ~app.py:93), explicitly clear `_META_CACHE` (belt-and-suspenders for coarse mtime). Create `tests/test_api_perf.py`: (1) `/api/meta` returns an identical dict across two calls AND reflects a change after the DB file is rewritten (new league appears); (2) `slugify`/`canonical_*` expose `cache_info()`; (3) best-effort: a `def` GET endpoint is registered as sync (e.g. assert via `app.routes` that list_cards endpoint is not a coroutine function) — or a light concurrency smoke. Use the test_api.py tmp_db/tmp_squads fixtures pattern.</action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_api_perf.py -q && FORCE_COLOR= NO_COLOR=1 python -m pytest -m golden --run-bench -q</verify>
  <acceptance_criteria>
    - app.py contains "_META_CACHE" and clears it in the refresh loop
    - get_meta response is byte-identical to before (golden api_meta.json passes)
    - tests/test_api_perf.py exists with ≥3 tests and passes
    - `pytest -m golden --run-bench` exits 0 (api_meta, api_build, api_upgrade, builder_matrix unchanged)
  </acceptance_criteria>
  <done>/api/meta cached + invalidated; perf tests green; golden byte-identical</done>
</task>

</tasks>

<verification>
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest -q` → all existing tests green
- [ ] `pytest tests/test_api_perf.py -q` green
- [ ] `pytest -m golden --run-bench` → byte-identical (api_meta/build/upgrade, builder_matrix)
- [ ] No change to compute_chemistry algorithm or any response shape
</verification>

<success_criteria>
- All tasks completed
- Handlers off the event loop; leaves memoized; /api/meta cached
- Zero behavior change: golden byte-identical, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-responsiveness/03-01-SUMMARY.md`
</output>
