---
phase: 01-benchmark-equivalence-harness
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/benchmarks/test_bench_db.py
  - tests/benchmarks/test_bench_refresh.py
  - tests/benchmarks/test_bench_builder.py
  - tests/benchmarks/test_bench_api.py
  - tests/benchmarks/README.md
  - .benchmarks/.gitkeep
autonomous: true
requirements: [BENCH-01, BENCH-02]
user_setup: []

must_haves:
  truths:
    - "`pytest -m benchmark --run-bench` runs benchmarks for db read/write, refresh, build/upgrade, and the key API endpoints and exits 0"
    - "Committed baselines captured on ro HEAD exist under .benchmarks/ via pytest-benchmark autosave"
    - "The regression gate command (--benchmark-compare --benchmark-compare-fail=mean:10%) is documented and runnable"
    - "Default `pytest -q` is unaffected (benchmarks skipped without --run-bench)"
  artifacts:
    - path: "tests/benchmarks/test_bench_db.py"
      provides: "find_all/find_by_id/upsert benchmarks over corpus + (live-gated) real DB read"
      min_lines: 25
    - path: "tests/benchmarks/test_bench_builder.py"
      provides: "build_squad / find_upgrades benchmarks"
      min_lines: 20
    - path: "tests/benchmarks/test_bench_api.py"
      provides: "TestClient benchmarks for /api/cards, /api/build, /api/upgrade, /api/meta"
      min_lines: 25
    - path: "tests/benchmarks/README.md"
      provides: "How to run benchmarks, save baselines, and run the regression gate"
      contains: "benchmark-compare-fail"
  key_links:
    - from: "tests/benchmarks/test_bench_api.py"
      to: "fc26.api.app.create_app"
      via: "TestClient(create_app(tmp_db, tmp_squads))"
      pattern: "create_app"
    - from: "tests/benchmarks/test_bench_refresh.py"
      to: "fc26.ingest.refresh.refresh_data"
      via: "injected fetch_html + stubbed sleep, manifest_path=None"
      pattern: "refresh_data"
---

<objective>
Build the speed-measurement half of the harness: pytest-benchmark suites for the hot paths Phases 2-5 will optimize (JSON db read/write, refresh pipeline, build/upgrade compute, key API endpoints), capture committed baselines on `ro` HEAD, and document the regression gate that fails when a path slows past +10% mean.

Purpose: BENCH-01 (baselines) + BENCH-02 (regression gate) — the "did it get faster / did anything regress" instrument.
Output: tests/benchmarks/test_bench_{db,refresh,builder,api}.py, committed .benchmarks/ baseline store, README with gate usage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-benchmark-equivalence-harness/01-RESEARCH.md
@.planning/phases/01-benchmark-equivalence-harness/01-01-PLAN.md
@fc26/db.py
@fc26/api/app.py
@fc26/builder/build.py
@fc26/builder/upgrade.py
@fc26/ingest/refresh.py
</context>

<threat_model>
No runtime surface added. Benchmarks read the public corpus and (optionally, live-gated) the public data/players.json — no secrets. The only operational note: the realistic-size db read benchmark must be gated like the existing `_REAL_DB`/`live` tests so CI/offline runs never depend on the mutable 4.4 MB file.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: db + refresh benchmarks (BENCH-01)</name>
  <files>tests/benchmarks/test_bench_db.py, tests/benchmarks/test_bench_refresh.py</files>
  <read_first>fc26/db.py, fc26/ingest/refresh.py, tests/benchmarks/corpus.py, tests/benchmarks/conftest.py, tests/test_refresh.py</read_first>
  <action>Create `test_bench_db.py` with `@pytest.mark.benchmark` tests using the `benchmark` fixture: bench `CardRepository.find_all()`, `find_by_id(<existing id>)`, and `upsert(<one card>)` over a corpus-seeded tmp repo (use load_corpus_repo). Add ONE `@pytest.mark.live` realistic-size read bench over the real `data/players.json` guarded exactly like test_api.py's `_REAL_DB` pattern (skip if the file is absent) so it only runs under --run-live. Create `test_bench_refresh.py` with a `@pytest.mark.benchmark` test calling `refresh_data(repo, fetch_html=offline_fetch(...), sleep=lambda _:None, on_progress=lambda _:None, manifest_path=None)` over a tmp corpus repo — fully offline, deterministic. Use injected fetch returning committed fixture HTML (reuse tests/fixtures where possible; if a full expand->enrich offline pass needs HTML the corpus can't supply, bench the enrich stage in isolation and note it in the README).</action>
  <verify>pytest tests/benchmarks/test_bench_db.py tests/benchmarks/test_bench_refresh.py -m benchmark --run-bench -q</verify>
  <acceptance_criteria>
    - `pytest tests/benchmarks/test_bench_db.py -m benchmark --run-bench` exits 0 and reports benchmark timings
    - test_bench_db.py uses the `benchmark` fixture and the corpus repo (not the live DB) for its default-gated benches
    - the real-DB read bench is marked `live` and skipped without --run-live
    - test_bench_refresh.py calls refresh_data with manifest_path=None and stubbed sleep (no real network, no real sleep)
    - `pytest -q` (no flags) still exits 0 and collects none of these
  </acceptance_criteria>
  <done>db + refresh benchmarks run under --run-bench, skipped by default</done>
</task>

<task type="auto">
  <name>Task 2: builder + API benchmarks (BENCH-01)</name>
  <files>tests/benchmarks/test_bench_builder.py, tests/benchmarks/test_bench_api.py</files>
  <read_first>fc26/builder/build.py, fc26/builder/upgrade.py, fc26/api/app.py, tests/test_api.py, tests/benchmarks/corpus.py</read_first>
  <action>Create `test_bench_builder.py` benching `build_squad(formation, pool, budget=..., objective="meta")` and `find_upgrades(...)` over the corpus pool for at least one formation from fc26/chem/formations.py with a budget large enough to exercise the greedy swap loop. Create `test_bench_api.py` building a `TestClient(create_app(tmp_db, tmp_squads))` where tmp_db is seeded from the corpus (mirror the tmp_db/tmp_squads fixtures in tests/test_api.py), and bench `GET /api/cards`, `GET /api/meta`, `POST /api/build`, and `POST /api/upgrade` (use a valid request body matching tests/test_api.py's VALID_SQUAD / build payload shape). All marked `@pytest.mark.benchmark`.</action>
  <verify>pytest tests/benchmarks/test_bench_builder.py tests/benchmarks/test_bench_api.py -m benchmark --run-bench -q</verify>
  <acceptance_criteria>
    - `pytest tests/benchmarks/test_bench_builder.py tests/benchmarks/test_bench_api.py -m benchmark --run-bench` exits 0
    - test_bench_api.py constructs TestClient(create_app(...)) with a corpus-seeded tmp_db
    - benches cover /api/cards, /api/meta, /api/build, /api/upgrade
    - `pytest -q` still exits 0 (none collected by default)
  </acceptance_criteria>
  <done>builder + API benchmarks run under --run-bench</done>
</task>

<task type="auto">
  <name>Task 3: Capture committed baselines + document the regression gate (BENCH-02)</name>
  <files>.benchmarks/.gitkeep, tests/benchmarks/README.md</files>
  <read_first>tests/benchmarks/test_bench_db.py, tests/benchmarks/test_bench_api.py, .planning/phases/01-benchmark-equivalence-harness/01-RESEARCH.md</read_first>
  <action>Run `pytest -m benchmark --run-bench --benchmark-autosave --benchmark-storage=.benchmarks` to capture the ro-HEAD baseline into `.benchmarks/`. Add `.benchmarks/.gitkeep` and ensure the generated baseline JSON under `.benchmarks/` is committed (these are the reference numbers later phases compare against). Write `tests/benchmarks/README.md` documenting: how to run benchmarks (`pytest -m benchmark --run-bench`), how baselines are stored (`--benchmark-autosave --benchmark-storage=.benchmarks`), and the regression gate command `pytest -m benchmark --run-bench --benchmark-storage=.benchmarks --benchmark-compare --benchmark-compare-fail=mean:10%` (fails the run if any benchmarked path's mean is >10% slower than the committed baseline). Confirm the gate runs green against its own freshly-saved baseline.</action>
  <verify>pytest -m benchmark --run-bench --benchmark-storage=.benchmarks --benchmark-compare --benchmark-compare-fail=mean:10% -q</verify>
  <acceptance_criteria>
    - `.benchmarks/` contains at least one saved baseline JSON (committed)
    - README.md contains the string "benchmark-compare-fail=mean:10%"
    - the gate command exits 0 when compared against the just-saved baseline (no self-regression)
    - `git diff --name-only` shows no files under fc26/
  </acceptance_criteria>
  <done>ro-HEAD baselines committed; regression gate documented and proven runnable</done>
</task>

</tasks>

<verification>
- [ ] `pytest -m benchmark --run-bench` exits 0 (all benches run)
- [ ] `.benchmarks/` baseline committed
- [ ] regression gate command runs green vs baseline
- [ ] `pytest -q` unchanged and green; no fc26/ changes
</verification>

<success_criteria>
- All tasks completed
- Baselines captured on ro HEAD and committed
- Regression gate documented + runnable
- Default pytest run unaffected; zero fc26/ changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-benchmark-equivalence-harness/01-02-SUMMARY.md`
</output>
