---
phase: 01-benchmark-equivalence-harness
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/benchmarks/test_golden_refresh.py
  - tests/benchmarks/test_golden_builder.py
  - tests/benchmarks/test_golden_api.py
  - tests/benchmarks/test_golden_cli.py
  - tests/benchmarks/golden/
  - tests/benchmarks/profile_refresh.py
  - tests/benchmarks/profile_builder.py
  - tests/benchmarks/README.md
autonomous: true
requirements: [BENCH-03, BENCH-04]
user_setup: []

must_haves:
  truths:
    - "Golden tests assert refresh output (players.json bytes + result tuples + progress sequence), build/upgrade result matrix, key API JSON, and CLI text are unchanged — over the frozen corpus, not the live DB"
    - "`pytest -m golden --run-bench` exits 0 against committed golden fixtures"
    - "REGEN_GOLDEN=1 regenerates fixtures intentionally; without it the tests assert equality"
    - "cProfile profiling entrypoints for refresh + build/upgrade exist and run sudo-free; py-spy usage documented"
    - "Default `pytest -q` is unaffected; zero fc26/ changes"
  artifacts:
    - path: "tests/benchmarks/test_golden_refresh.py"
      provides: "Deterministic refresh equivalence (bytes/tuples/progress) over corpus"
      min_lines: 20
    - path: "tests/benchmarks/test_golden_api.py"
      provides: "API JSON response equivalence via TestClient"
      min_lines: 20
    - path: "tests/benchmarks/test_golden_cli.py"
      provides: "CLI stdout equivalence via CliRunner"
      min_lines: 15
    - path: "tests/benchmarks/profile_refresh.py"
      provides: "cProfile wrapper for the refresh hot path"
      min_lines: 10
    - path: "tests/benchmarks/README.md"
      provides: "Profiling docs (cProfile default; py-spy manual w/ macOS sudo caveat)"
      contains: "cProfile"
  key_links:
    - from: "tests/benchmarks/test_golden_api.py"
      to: "fc26.api.app.create_app"
      via: "TestClient(create_app(tmp_db, tmp_squads)) golden of JSON responses"
      pattern: "create_app"
    - from: "tests/benchmarks/test_golden_cli.py"
      to: "fc26.cli.app"
      via: "CliRunner().invoke(app, [...]) stdout compared to golden"
      pattern: "CliRunner"
---

<objective>
Build the behavior-equivalence half of the harness: golden-output tests that capture footie's current outputs over the frozen corpus and assert they are byte/structure-identical after any later optimization, plus documented cProfile/py-spy profiling entrypoints for the refresh and build/upgrade hot paths.

Purpose: BENCH-03 (golden/equivalence — the zero-behavior-change gate that every Phase 2-5 change must pass) + BENCH-04 (profiling entrypoints to find what to optimize).
Output: tests/benchmarks/test_golden_{refresh,builder,api,cli}.py + committed golden fixtures, profile_refresh.py, profile_builder.py, README profiling section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-benchmark-equivalence-harness/01-RESEARCH.md
@.planning/phases/01-benchmark-equivalence-harness/01-01-PLAN.md
@fc26/db.py
@fc26/api/app.py
@fc26/ingest/refresh.py
@fc26/cli.py
</context>

<threat_model>
No runtime surface added. Golden fixtures contain only public FUT card data + deterministic API/CLI output — review before commit to ensure no secrets/PII. py-spy on macOS requires sudo; it is documented as a manual-only step and is NEVER part of the automated test run (cProfile covers BENCH-04 sudo-free).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh + builder golden (BENCH-03)</name>
  <files>tests/benchmarks/test_golden_refresh.py, tests/benchmarks/test_golden_builder.py, tests/benchmarks/golden/</files>
  <read_first>fc26/ingest/refresh.py, fc26/db.py, fc26/builder/build.py, fc26/builder/upgrade.py, tests/benchmarks/corpus.py</read_first>
  <action>Create `test_golden_refresh.py` (`@pytest.mark.golden`): run `refresh_data(repo, fetch_html=offline_fetch(...), sleep=lambda _:None, on_progress=collector, manifest_path=None)` over a corpus-seeded tmp repo, then assert via golden_check: (a) the resulting players.json RAW BYTES (db._save is deterministic: id-sorted, indent=2, trailing newline, ensure_ascii=False — so byte compare is valid), (b) the RefreshResult counts/ids tuple, (c) the captured on_progress message sequence. Create `test_golden_builder.py` (`@pytest.mark.golden`): run a small deterministic matrix of `build_squad`/`find_upgrades` over the corpus (a few formations × objectives "meta"/"rating" × 2-3 budgets — subset to keep it quick) and golden_check the resulting slot_cards ids + total_cost + swap list. Generate fixtures once with REGEN_GOLDEN=1, eyeball them, commit under tests/benchmarks/golden/.</action>
  <verify>pytest tests/benchmarks/test_golden_refresh.py tests/benchmarks/test_golden_builder.py -m golden --run-bench -q</verify>
  <acceptance_criteria>
    - `pytest tests/benchmarks/test_golden_refresh.py tests/benchmarks/test_golden_builder.py -m golden --run-bench` exits 0
    - golden fixtures for refresh (bytes/tuples/progress) and builder matrix exist under tests/benchmarks/golden/
    - test_golden_refresh.py calls refresh_data with manifest_path=None (no nondeterministic refreshed_at)
    - re-running the tests twice in a row is stable (deterministic)
    - `pytest -q` still exits 0 (none collected by default)
  </acceptance_criteria>
  <done>Refresh + builder equivalence locked to committed golden over the corpus</done>
</task>

<task type="auto">
  <name>Task 2: API + CLI golden (BENCH-03)</name>
  <files>tests/benchmarks/test_golden_api.py, tests/benchmarks/test_golden_cli.py, tests/benchmarks/golden/</files>
  <read_first>fc26/api/app.py, fc26/cli.py, tests/test_api.py, tests/test_cli.py, tests/benchmarks/corpus.py</read_first>
  <action>Create `test_golden_api.py` (`@pytest.mark.golden`): build TestClient(create_app(corpus_tmp_db, tmp_squads)) and golden_check the JSON of `GET /api/cards` (deterministic ordering — if the endpoint sorts, rely on it; else sort in the test before compare), `GET /api/meta`, and a representative `POST /api/build` + `POST /api/upgrade` response. Preserve the exact response shape (note the /api/meta version-filter quirk from RESEARCH/BACKEND-COMPUTE: versions unfiltered, leagues/nations/clubs filter falsy — golden captures current truth). Create `test_golden_cli.py` (`@pytest.mark.golden`) using `typer.testing.CliRunner` (mirror tests/test_cli.py): golden_check_text the stdout of a few deterministic commands over a corpus DB (e.g. `search`, `list --sort ovr --json`, `show <id> --json`). Generate with REGEN_GOLDEN=1, commit fixtures.</action>
  <verify>pytest tests/benchmarks/test_golden_api.py tests/benchmarks/test_golden_cli.py -m golden --run-bench -q</verify>
  <acceptance_criteria>
    - `pytest tests/benchmarks/test_golden_api.py tests/benchmarks/test_golden_cli.py -m golden --run-bench` exits 0
    - test_golden_api.py uses TestClient(create_app(...)) and covers /api/cards, /api/meta, /api/build, /api/upgrade
    - test_golden_cli.py uses CliRunner and golden_check_text on stdout
    - golden fixtures committed under tests/benchmarks/golden/
    - `pytest -q` still exits 0
  </acceptance_criteria>
  <done>API + CLI output equivalence locked to committed golden</done>
</task>

<task type="auto">
  <name>Task 3: Profiling entrypoints + docs (BENCH-04)</name>
  <files>tests/benchmarks/profile_refresh.py, tests/benchmarks/profile_builder.py, tests/benchmarks/README.md</files>
  <read_first>tests/benchmarks/README.md, fc26/ingest/refresh.py, fc26/builder/build.py, fc26/builder/upgrade.py, tests/benchmarks/corpus.py, .planning/phases/01-benchmark-equivalence-harness/01-RESEARCH.md</read_first>
  <action>Create `profile_refresh.py`: a runnable script that cProfiles `refresh_data` over the corpus (offline fetch, stubbed sleep) and prints top cumulative-time functions (pstats sort by 'cumtime', print_stats(30)). Create `profile_builder.py`: cProfile `build_squad`/`find_upgrades` over the corpus. Both must run sudo-free via `python tests/benchmarks/profile_refresh.py`. Extend `tests/benchmarks/README.md` with a "Profiling" section documenting: cProfile (default, no sudo) via these scripts; py-spy as the live/sampling option with the macOS caveat (requires sudo; SIP blocks /usr/bin python — use the Homebrew interpreter at /opt/homebrew/bin/python3) and an example `sudo py-spy record -o profile.svg -- python tests/benchmarks/profile_refresh.py`.</action>
  <verify>python tests/benchmarks/profile_refresh.py && python tests/benchmarks/profile_builder.py</verify>
  <acceptance_criteria>
    - `python tests/benchmarks/profile_refresh.py` exits 0 and prints a cProfile table (no sudo)
    - `python tests/benchmarks/profile_builder.py` exits 0
    - README.md "Profiling" section contains "cProfile" and "py-spy" and the macOS sudo caveat
    - `git diff --name-only` shows no files under fc26/
  </acceptance_criteria>
  <done>cProfile entrypoints runnable sudo-free; py-spy documented</done>
</task>

</tasks>

<verification>
- [ ] `pytest -m golden --run-bench` exits 0 against committed fixtures
- [ ] golden tests are deterministic (stable across reruns)
- [ ] `python tests/benchmarks/profile_refresh.py` + `profile_builder.py` run sudo-free
- [ ] `pytest -q` unchanged and green; no fc26/ changes
</verification>

<success_criteria>
- All tasks completed
- Golden equivalence covers refresh/builder/API/CLI over the frozen corpus
- Profiling entrypoints documented + runnable
- Default pytest run unaffected; zero fc26/ changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-benchmark-equivalence-harness/01-03-SUMMARY.md`
</output>
