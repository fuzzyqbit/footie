---
phase: 01-benchmark-equivalence-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/benchmarks/__init__.py
  - tests/benchmarks/conftest.py
  - tests/benchmarks/corpus.py
  - tests/benchmarks/golden/corpus.json
autonomous: true
requirements: [BENCH-01]
user_setup: []

must_haves:
  truths:
    - "Default `pytest -q` still passes with zero failures and stays offline (no benchmark/golden tests run unless --run-bench is passed)"
    - "A deterministic frozen card corpus exists, decoupled from the live data/players.json"
    - "No file under fc26/ is modified (additive test tooling only)"
  artifacts:
    - path: "tests/benchmarks/corpus.py"
      provides: "Deterministic corpus loader + offline fetch_html helper + golden_check/golden_check_text helpers"
      min_lines: 30
    - path: "tests/benchmarks/conftest.py"
      provides: "Shared benchmark/golden fixtures (corpus repo, REGEN_GOLDEN handling)"
      min_lines: 15
    - path: "tests/benchmarks/golden/corpus.json"
      provides: "Frozen ~30-60 card pool, schema_version 1"
      contains: "schema_version"
    - path: "pyproject.toml"
      provides: "pytest-benchmark + py-spy dev deps; benchmark/golden markers"
      contains: "pytest-benchmark"
  key_links:
    - from: "tests/conftest.py"
      to: "benchmark/golden markers"
      via: "--run-bench option + skip logic mirroring --run-live"
      pattern: "run-bench"
---

<objective>
Lay the foundation for the benchmark + golden-output harness: add dev dependencies and markers, extend the existing pytest skip machinery with a `--run-bench` opt-in, and create a deterministic frozen card corpus plus shared helpers that every benchmark and golden test will build on.

Purpose: Phase 1 is the measurement instrument + safety net that gates Phases 2-5. This plan creates the scaffolding so benchmarks (Plan 02) and golden checks (Plan 03) are deterministic, offline, and never pollute the default `pytest` run.
Output: pyproject.toml deps+markers, tests/conftest.py extension, tests/benchmarks/{conftest.py,corpus.py,golden/corpus.json}.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-benchmark-equivalence-harness/01-RESEARCH.md
@tests/conftest.py
@tests/test_api.py
@pyproject.toml
@fc26/db.py
</context>

<threat_model>
Phase 1 adds no runtime surface (no endpoints, auth, inputs, network, crypto, persistence). Only relevant threats:
- **Supply-chain (typosquatted dev dep)** — `pytest-benchmark` and `py-spy` were verified on PyPI in RESEARCH.md (pytest-benchmark 5.2.3 ionelmc/pytest-benchmark; py-spy 0.4.2 benfred/py-spy). Mitigation: Task 1 runs `pip index versions` on each before install to confirm they resolve to the known packages; block: high severity (none expected).
- **Secrets in committed fixtures** — corpus.json contains only public FUT card data. Mitigation: no credentials/PII; reviewed before commit.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark/profiling dev deps + markers to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>In `[project.optional-dependencies]`, extend `dev` from `["pytest>=8", "pytest-cov>=5"]` to also include `"pytest-benchmark>=5,<6"` and `"py-spy>=0.4"`. In `[tool.pytest.ini_options]`, change `markers = ["live: hits the real network (opt-in via --run-live)"]` to a list that ALSO contains `"benchmark: performance benchmark (opt-in via --run-bench)"` and `"golden: golden-output equivalence check (opt-in via --run-bench)"`. Before editing, run `pip index versions pytest-benchmark` and `pip index versions py-spy` to confirm both packages resolve; if either does not resolve, stop and flag (do not invent an alternative). Then run `pip install -e ".[dev]"` to install. Do NOT touch the runtime `dependencies` list.</action>
  <verify>pip show pytest-benchmark >/dev/null && python -c "import pytest_benchmark"</verify>
  <acceptance_criteria>
    - pyproject.toml `dev` extra contains "pytest-benchmark>=5,<6" and "py-spy>=0.4"
    - pyproject.toml markers list contains "benchmark:" and "golden:"
    - `pip show pytest-benchmark` exits 0
    - runtime `dependencies` array is unchanged (still typer/rich/httpx/selectolax/fastapi/uvicorn)
  </acceptance_criteria>
  <done>pytest-benchmark + py-spy installed; benchmark/golden markers registered; runtime deps untouched</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/conftest.py with --run-bench opt-in (mirror --run-live)</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py</read_first>
  <action>Mirror the existing `--run-live`/`live` pattern exactly. In `pytest_addoption`, add a second `parser.addoption("--run-bench", action="store_true", default=False, help="run benchmark + golden tests")`. In `pytest_collection_modifyitems`, after the existing live handling, add: if NOT `config.getoption("--run-bench")`, build `skip_bench = pytest.mark.skip(reason="benchmark/golden: pass --run-bench to run")` and add it to any item whose keywords include "benchmark" or "golden". Keep the existing live logic intact and independent (live still gated by --run-live).</action>
  <verify>pytest -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - tests/conftest.py contains 'addoption("--run-bench"' (or equivalent --run-bench registration)
    - tests/conftest.py contains "benchmark" and "golden" in the skip-keyword check
    - `pytest -q` still exits 0 with the existing ~346 tests (no benchmark/golden collected because they don't exist yet)
    - existing `--run-live`/`live` logic still present and unchanged
  </acceptance_criteria>
  <done>--run-bench gates benchmark/golden marks; default run unaffected</done>
</task>

<task type="auto">
  <name>Task 3: Create deterministic frozen corpus + shared benchmark/golden helpers</name>
  <files>tests/benchmarks/__init__.py, tests/benchmarks/corpus.py, tests/benchmarks/conftest.py, tests/benchmarks/golden/corpus.json</files>
  <read_first>fc26/db.py, fc26/api/app.py, fc26/chem/formations.py, tests/test_api.py, .planning/phases/01-benchmark-equivalence-harness/01-RESEARCH.md</read_first>
  <action>Create `tests/benchmarks/golden/corpus.json`: a `{"schema_version": 1, "cards": [...]}` file with ~30-60 cards, deterministic (cards sorted by id, fixed field order matching fc26/db.py card_to_dict). Coverage requirements: at least 2 priced+fully-statted cards for EVERY position used across FORMATIONS in fc26/chem/formations.py (so build_squad can seed + swap), spanning ≥2 leagues and ≥2 nations, every card with non-null price and all six face stats (pac/sho/pas/dri/def_/phy) so `_usable` in fc26/builder/build.py passes. Mirror the field shape of `_card_dict` in tests/test_api.py (face dict uses key `def_`). Create `tests/benchmarks/corpus.py` with: `corpus_path()` returning the golden/corpus.json path; `load_corpus_repo(tmp_path)` that copies corpus.json into a tmp players.json and returns a `fc26.db.CardRepository`; `offline_fetch(mapping)` returning a `fetch_html(url)->str` that serves committed fixture HTML / raises for unknown urls (offline, deterministic); `golden_check(name, obj)` and `golden_check_text(name, text)` helpers that compare a value against `tests/benchmarks/golden/<name>` and, when env `REGEN_GOLDEN=1`, (re)write it instead of asserting. JSON golden writes use `json.dumps(..., ensure_ascii=False, indent=2, sort_keys=True)` for stable diffs. Create `tests/benchmarks/__init__.py` (empty) and `tests/benchmarks/conftest.py` exposing `corpus_repo` and a `tmp_repo` fixture built via `load_corpus_repo`.</action>
  <verify>python -c "import json,sys; d=json.load(open('tests/benchmarks/golden/corpus.json')); assert d['schema_version']==1; print(len(d['cards']),'cards')"</verify>
  <acceptance_criteria>
    - tests/benchmarks/golden/corpus.json parses as JSON with schema_version==1 and >=30 cards
    - every card in corpus.json has a non-null "price" and a "face" with non-null pac/sho/pas/dri/def_/phy
    - `python -c "from fc26.db import CardRepository, card_from_dict; import json; [card_from_dict(c) for c in json.load(open('tests/benchmarks/golden/corpus.json'))['cards']]"` exits 0 (every card is a valid Card)
    - tests/benchmarks/corpus.py defines load_corpus_repo, offline_fetch, golden_check, golden_check_text
    - tests/benchmarks/corpus.py honors REGEN_GOLDEN to rewrite golden instead of asserting
    - `pytest -q` still exits 0 (new files collect nothing by default)
  </acceptance_criteria>
  <done>Deterministic corpus + reusable offline-fetch + golden helpers ready for Plans 02/03</done>
</task>

</tasks>

<verification>
- [ ] `pip install -e ".[dev]"` succeeds; `python -c "import pytest_benchmark"` works
- [ ] `pytest -q` exits 0 with no new tests collected (default run unchanged)
- [ ] `git diff --name-only` shows NO files under fc26/
- [ ] corpus.json loads as valid Cards covering all formation positions
</verification>

<success_criteria>
- All tasks completed
- Default pytest run stays fast/offline and green
- Zero fc26/ runtime changes
- Foundation (deps, markers, --run-bench, corpus, helpers) ready for benchmarks + golden
</success_criteria>

<output>
After completion, create `.planning/phases/01-benchmark-equivalence-harness/01-01-SUMMARY.md`
</output>
