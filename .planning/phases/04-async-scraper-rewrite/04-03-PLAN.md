---
phase: 04-async-scraper-rewrite
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - fc26/cli.py
  - fc26/api/app.py
  - tests/benchmarks/test_bench_refresh.py
files_created:
  - (none — extends existing bench file)
autonomous: true
requirements: [SCRAPE-01, SCRAPE-04]
user_setup: []

must_haves:
  truths:
    - "The CLI refresh/enrich/expand/images commands run the async pipeline via asyncio.run(*_async(...)) inside the existing repo.batch(); command output text is unchanged"
    - "The FastAPI auto-refresh loop runs refresh_data_async via await asyncio.to_thread(lambda: asyncio.run(...)) — a fresh loop in a worker thread, never nested in uvicorn's loop, keeping all scrape + file I/O off the server loop; _META_CACHE bust unchanged"
    - "A simulated-latency benchmark proves async refresh wall-clock is materially below the sequential sum-of-sleeps (the concurrency win the offline write-bench cannot show)"
    - "The Phase 1 golden is byte-identical and the benchmark gate shows no regression vs 0003; baselines re-saved as 0004"
  artifacts:
    - path: "fc26/cli.py"
      provides: "CLI commands wired to asyncio.run(async pipeline)"
      contains: "asyncio.run"
    - path: "fc26/api/app.py"
      provides: "auto-refresh runs refresh_data_async via to_thread(asyncio.run)"
      contains: "refresh_data_async"
    - path: "tests/benchmarks/test_bench_refresh.py"
      provides: "simulated-latency async-vs-sequential benchmark"
      contains: "asyncio.run"
  key_links:
    - from: "_refresh_loop"
      to: "refresh_data_async"
      via: "await asyncio.to_thread(lambda: asyncio.run(refresh_data_async(...)))"
      pattern: "to_thread"
    - from: "cli.refresh"
      to: "refresh_data_async"
      via: "asyncio.run inside repo.batch()"
      pattern: "asyncio.run"
---

<objective>
Flip ingest to the async pipeline at the two entry points (sync CLI, FastAPI background loop) without nested loops, and prove the speedup deterministically. Output text and on-disk bytes stay identical (golden is the gate); the simulated-latency benchmark shows the concurrency win that the offline write-bench structurally cannot.

Purpose: SCRAPE-01 (real refresh routes through the shared AsyncClient), SCRAPE-04 (CLI `asyncio.run` + API `to_thread(asyncio.run)` integration, no nested loops, byte-identical).
Output: wired `cli.py` + `app.py`, simulated-latency bench in `test_bench_refresh.py`, re-baselined `.benchmarks/0004`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/ASYNC-SCRAPING.md
@.planning/phases/04-async-scraper-rewrite/04-RESEARCH.md
@fc26/cli.py
@fc26/api/app.py
@fc26/ingest/refresh.py
@fc26/ingest/web_async.py
@tests/benchmarks/test_bench_refresh.py
@tests/benchmarks/corpus.py
@tests/conftest.py
</context>

<threat_model>
The only new hazard is event-loop nesting. Guard: CLI Typer commands are sync (no running loop) → `asyncio.run` is safe; the FastAPI loop keeps the existing `asyncio.to_thread` boundary and calls `asyncio.run` INSIDE the worker thread (no loop there) → fresh loop, never nested in uvicorn's, and all blocking upsert file I/O stays off the server loop exactly as today. No `nest_asyncio`. Politeness/output unchanged (inherited from Plans 04-01/02). CancelledError handling in `_refresh_loop` still works (cancelling the outer task cancels the `to_thread` await).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire the CLI commands to the async pipeline (SCRAPE-01/04)</name>
  <files>fc26/cli.py</files>
  <read_first>fc26/cli.py (refresh/enrich/expand/images cmds ~184-311), fc26/ingest/refresh.py, fc26/ingest/web_async.py, .planning/research/ASYNC-SCRAPING.md (§"CLI — clean asyncio.run")</read_first>
  <action>
Add `import asyncio` to cli.py. Convert the four ingest commands to drive the async siblings, keeping ALL output text, error handling (`except FC26Error: _fail(...)`), option signatures, and the `with repo.batch():` wrapper identical:
- `refresh` (282): replace `refresh_data(repo, ...)` with `asyncio.run(refresh_data_async(repo, min_ovr=min_ovr, concurrency=4, min_interval=1.0, on_progress=console.print, enrich_limit=limit, manifest_path=db.parent / "last_refresh.json"))`. (refresh_data_async owns its AsyncFetcher; no batch() needed here — it batches internally, same as sync refresh_data.)
- `enrich` (185): inside `with repo.batch():`, `result = asyncio.run(_enrich_async(repo, refresh=refresh, limit=limit))` where the call constructs an AsyncFetcher and runs `enrich_cards_async`. Provide a tiny local async helper, e.g. `async def _run(): async with AsyncFetcher(concurrency=4, min_interval=1.0) as f: return await enrich_cards_async(repo, fetcher=f, on_progress=console.print, refresh=refresh, limit=limit)` then `result = asyncio.run(_run())`.
- `expand` (215): same pattern with `expand_cards_async(repo, min_ovr=min_ovr, fetcher=f, on_progress=console.print, max_pages=max_pages)`.
- `images` (245): same pattern with `upgrade_card_images_async(repo, fetcher=f, on_progress=console.print, refresh=refresh, limit=limit)`. Map the existing `--workers` option to the fetcher's concurrency: `AsyncFetcher(concurrency=workers if workers and workers > 1 else 4, min_interval=1.0)` — preserves user intent (`--workers N` still means "fetch N at a time"). **Behavioral note (document in summary):** under async, output is always card-order (the deterministic `workers=1` order), so `--workers N>1` no longer yields the old `as_completed` order. This is identical to the previous `--workers 1` (default) output and only differs from the previously non-deterministic `--workers N>1` ordering — see 04-02 Task 2.

**Batch nesting (must be unambiguous):** standalone `enrich`/`expand`/`images` do NOT batch internally → keep the outer `with repo.batch():` in the CLI exactly as today. Only `refresh_data_async` batches internally (its 04-02 Task 4 body wraps `with repo.batch():`), so the `refresh` CLI command must call `asyncio.run(refresh_data_async(...))` with NO outer `repo.batch()` (matching today's sync `refresh` which also has none — `cli.py:294-304`). Do not double-nest batch.

Import `AsyncFetcher` from `.ingest.web_async` and the `*_async` functions from their modules. Keep the existing sync imports too (don't remove them — harmless, and avoids churn). Verify the console output strings are byte-for-byte the same as before.
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_cli.py -q</verify>
  <acceptance_criteria>
    - cli.py imports asyncio and the *_async functions + AsyncFetcher
    - refresh/enrich/expand/images run via asyncio.run(...); output text + error handling + batch() unchanged
    - tests/test_cli.py green (CLI behavior/text identical)
  </acceptance_criteria>
  <done>CLI drives the async pipeline with identical output</done>
</task>

<task type="auto">
  <name>Task 2: Wire the FastAPI auto-refresh loop (SCRAPE-04, no nested loop)</name>
  <files>fc26/api/app.py</files>
  <read_first>fc26/api/app.py (_refresh_loop ~83-112, imports ~37-40), fc26/ingest/refresh.py, .planning/research/ASYNC-SCRAPING.md (§"FastAPI — keep the to_thread boundary")</read_first>
  <action>
In `app.py`, import `refresh_data_async` from `..ingest.refresh` (keep `refresh_data` import too if still referenced elsewhere; if not, replace it). In `_refresh_loop`, replace the `await asyncio.to_thread(refresh_data, repo, min_ovr=..., fetch_html=fetch_html, sleep=jittered_sleep, manifest_path=...)` call (`:97-101`) with:
`result = await asyncio.to_thread(lambda: asyncio.run(refresh_data_async(repo, min_ovr=min_ovr, concurrency=4, min_interval=1.0, manifest_path=db_path.parent / "last_refresh.json")))`.
This runs a fresh event loop inside the worker thread (no loop there → never nested in uvicorn's), keeping the whole scrape incl. upsert file I/O off the server loop exactly as today. Leave the surrounding `try/except asyncio.CancelledError: raise` / `except Exception:` logging and the `with _META_LOCK: _META_CACHE.clear()` bust UNCHANGED. The `fetch_html`/`jittered_sleep` imports may become unused for the loop — remove them ONLY if nothing else in app.py uses them (check; `fetch_html` may still be imported — leave imports that are still referenced).
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/test_api.py tests/test_refresh.py -q && FORCE_COLOR= NO_COLOR=1 python -c "from fc26.api.app import create_app; print('ok')"</verify>
  <acceptance_criteria>
    - app.py _refresh_loop calls refresh_data_async via await asyncio.to_thread(lambda: asyncio.run(...))
    - no nest_asyncio; CancelledError handling + _META_CACHE bust unchanged
    - tests/test_api.py + tests/test_refresh.py green; create_app imports clean
  </acceptance_criteria>
  <done>API auto-refresh runs the async pipeline in a worker-thread loop, off the server loop</done>
</task>

<task type="auto">
  <name>Task 3: Simulated-latency benchmark — prove async ≪ sequential (SCRAPE-01)</name>
  <files>tests/benchmarks/test_bench_refresh.py</files>
  <read_first>tests/benchmarks/test_bench_refresh.py, tests/benchmarks/corpus.py, fc26/ingest/web_async.py, fc26/ingest/refresh.py</read_first>
  <action>
Add a NEW `@pytest.mark.benchmark` test to test_bench_refresh.py that proves the concurrency win the existing write-bench cannot (it excludes network). The offline bench must NOT hit the network and must be deterministic. Approach:
- Build a stub async fetcher whose `fetch(url)` does `await asyncio.sleep(LAT)` (e.g. LAT=0.005) then returns a fixture HTML for that url — i.e. a known number of fetches each costing a fixed simulated latency. Use a small synthetic set of N cards/pages so the run is quick (e.g. enrich over ~30 corpus cards needing M player-page fetches), monkeypatching the parse functions so no real HTML is needed.
- Bench the async path: `benchmark.pedantic(lambda: asyncio.run(_run_async()), rounds=...)` where `_run_async` uses the stub fetcher with `concurrency=8`.
- Assert the SPEEDUP structurally with an **analytic** (not timed) sequential baseline: compute `sequential_baseline = N_fetches * LAT` as a constant (do NOT run a real sequential timed pass — that adds N×LAT of suite wall-clock and CI jitter). Capture the async wall-clock from the benchmark stats (e.g. `benchmark.stats.stats.mean` or a manual `time.perf_counter()` around one `asyncio.run`) and assert `async_wall_clock < 0.4 * sequential_baseline`. Use a generous margin so the test fails only if concurrency regresses to serial, not on CI jitter. Document the math + the chosen N_fetches and LAT in a comment (sequential ≈ N×LAT; async ≈ ceil(N/concurrency)×LAT + overhead; with concurrency=8 the 0.4× threshold has wide headroom).
- This bench records under the `benchmark` marker so it participates in `--benchmark-compare`. It is offline + deterministic.
Do NOT modify the existing two upsert benchmarks.
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest tests/benchmarks/test_bench_refresh.py -q --run-bench</verify>
  <acceptance_criteria>
    - test_bench_refresh.py has a new @pytest.mark.benchmark test that runs the async pipeline over a simulated-latency stub fetcher
    - it asserts async wall-clock is materially below the sequential sum-of-sleeps (concurrency proven), offline + deterministic
    - existing upsert benchmarks unchanged and still pass
  </acceptance_criteria>
  <done>The async concurrency win is captured by a deterministic offline benchmark</done>
</task>

<task type="auto">
  <name>Task 4: Golden + benchmark gate + re-baseline 0004 (BENCH-02/03 regression)</name>
  <files>(no source edits — verification + baseline commit)</files>
  <read_first>tests/conftest.py, .benchmarks/ (existing 0001-0003), .planning/phases/04-async-scraper-rewrite/04-VALIDATION.md</read_first>
  <action>
Run the full gates and re-baseline:
1. `FORCE_COLOR= NO_COLOR=1 pytest -q` — whole default suite green (incl. new test_web_async + test_ingest_async).
2. `FORCE_COLOR= NO_COLOR=1 pytest -m golden --run-bench -q` — byte-identical (refresh_players.json, refresh_readback.json, api/builder/cli goldens unchanged). If ANY golden mismatches, STOP — that is a behavior change and must be fixed in 04-02, not regenerated.
3. `FORCE_COLOR= NO_COLOR=1 pytest -m benchmark --run-bench --benchmark-storage=.benchmarks --benchmark-compare=0003 --benchmark-compare-fail=mean:10%` — this gate validates **non-regression on the EXISTING benchmark names only** (the two upsert write-benches present in 0003). The two-part nature is important: (a) **regression guard** = this compare-fail call, run against 0003; (b) **speedup proof for SCRAPE-01** = the in-test assertion inside the new simulated-latency bench (Task 3: `async_wall_clock < threshold × sequential_baseline`), NOT `--benchmark-compare`. The new simulated-latency test has NO prior baseline in 0003, so `--benchmark-compare` cannot and does not prove the concurrency win for it — that proof lives entirely in the test body. Do not expect the compare gate to flag the async speedup.
   - **Baseline note (ROADMAP SC-1 says "Phase 2 baseline"):** Phase 2's baseline is 0002; Phase 3 re-baselined to 0003. We compare against **0003** because it is a strict superset of 0002 (same write-bench names, Phase 3 only added API/builder benches and never regressed the write path), so 0003 is the conservative, most-recent regression reference. The existing offline write-bench measures only write cost — it structurally cannot show the fetch-concurrency win; that is exactly why Task 3's simulated-latency bench exists.
4. Re-save the baseline as 0004: `FORCE_COLOR= NO_COLOR=1 pytest -m benchmark --run-bench --benchmark-save=0004` (writes `.benchmarks/.../0004_*.json`, now including the simulated-latency bench so Phase 5 can compare against it). Commit it so Phase 5 compares against 0004.
Record the before/after refresh numbers (simulated-latency async wall-clock vs the analytic sequential sum = N_fetches × LAT) in the summary.
  </action>
  <verify>FORCE_COLOR= NO_COLOR=1 python -m pytest -q && FORCE_COLOR= NO_COLOR=1 python -m pytest -m golden --run-bench -q</verify>
  <acceptance_criteria>
    - `pytest -q` exits 0 (whole suite green)
    - `pytest -m golden --run-bench` exits 0 (byte-identical; nothing regenerated)
    - benchmark compare vs 0003 shows no regression > 10% on the existing upsert benches (the SCRAPE-01 speedup proof is the in-test assertion, not the compare gate); 0004 baseline saved + committed
  </acceptance_criteria>
  <done>All gates green; async path byte-identical and proven faster; baseline 0004 committed</done>
</task>

</tasks>

<verification>
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest -q` → whole suite green (cli, api, ingest, async)
- [ ] `FORCE_COLOR= NO_COLOR=1 pytest -m golden --run-bench` → byte-identical (no golden regenerated)
- [ ] benchmark gate vs 0003 no regression; simulated-latency bench shows async ≪ sequential; 0004 saved
- [ ] CLI uses asyncio.run; API uses to_thread(asyncio.run); no nested loop; no nest_asyncio
</verification>

<success_criteria>
- Async pipeline is the default at both entry points (CLI + FastAPI), output byte-identical
- Concurrency win proven by a deterministic offline benchmark
- Golden green, benchmark re-baselined 0004, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/04-async-scraper-rewrite/04-03-SUMMARY.md`
</output>
